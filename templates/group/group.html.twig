{% extends 'base.html.twig' %}

{% block title %}Create group{% endblock %}

{% block body %}
    <h1>Create group</h1>
{{ dump() }}
    {{ form_errors(groupForm) }}
{#    {{ form(groupForm) }}#}
    {{ form_start(groupForm) }}
{#    {{ csrf_token('groupadd') }}#}
        {{ form_row(groupForm.name, {
            label: 'Name group'
        }) }}
{#    {% if groupForm.vars.data.id != null %}#}
        {{ form_row(groupForm.users, {
            label: 'User',

        }) }}
{#    {% endif %}#}
{#    {{ form_rest(groupForm) }}#}

    <button type="submit" class="btn btn-dark">Add</button>
    {{ form_end(groupForm) }}
{% endblock %}